# AGENTS.md — apps/web/

> Contexto específico do frontend React. Não repete o root AGENTS.md.
> **Atualizado:** 2026-05-24

---

## Entrada

`src/main.tsx` → `src/App.tsx` → React Router v6 → módulos em `src/modules/`

Providers aninhados (App.tsx):

```
QueryClientProvider → ThemeProvider → BrowserRouter → AuthProvider → ModulesProvider → AppRoutes
```

Todas as rotas: `src/routes/AppRoutes.tsx` — lazy-loaded, usa `protectedRoute()` helper.

---

## Convenções Obrigatórias

| O quê            | Como                                              | Nunca                              |
| ---------------- | ------------------------------------------------- | ---------------------------------- |
| Requisições HTTP | `apiClient` de `lib/api/apiClient.ts`             | `fetch` ou `axios` direto          |
| Datas            | `lib/utils/date.utils.ts`                         | `date-fns` importado direto        |
| Auth state       | `useAuth()` de `contexts/AuthContext.tsx`         | localStorage manual                |
| Toast/notif      | `useToast` de `@orthoplus/core-hooks`             | `alert()`                          |
| Componentes UI   | `@orthoplus/core-ui` (Button, Card, Input...)     | shadcn local se já existir no core |
| Estado global    | Zustand stores em módulos (`use{Modulo}Store.ts`) | Context para estado server-side    |
| Estado servidor  | React Query (`useQuery`/`useMutation`)            | useState para dados async          |

---

## Arquitetura (Parcial Clean Arch)

Aplicada apenas nos módulos que já a possuem. **Não adicionar camadas onde não existem.**

```
src/
├── domain/          # 24 entidades, 19 interfaces de repositório, value-objects
├── application/
│   └── use-cases/   # 60 use-cases (concentrados em financeiro, agenda, estoque, crypto)
├── infrastructure/
│   └── repositories/# 15 implementações concretas
├── modules/         # 37 módulos UI (~695 arquivos)
├── hooks/           # hooks globais (useHotkeys, useGlobalSearch, usePDV, etc.)
│   └── api/         # hooks de API (useModulos, usePacientes, useInventario, etc.)
├── contexts/        # AuthContext, ModulesContext, ThemeContext
├── routes/          # AppRoutes.tsx (todas as rotas)
├── components/      # componentes compartilhados (~1116 arquivos)
└── presentation/    # camada de apresentação (8 arquivos)
```

Módulos **sem** Clean Arch (maioria): usam hooks + apiClient direto. Seguir o padrão existente do módulo ao editar.

---

## Estrutura de Módulo (Típica)

```
src/modules/{modulo}/
├── components/
├── hooks/           # use{Modulo}.ts, use{Modulo}Store.ts (Zustand)
├── pages/ ou ui/pages/
├── types.ts
└── index.ts         # re-exports (NEM TODOS OS MÓDULOS TÊM — ver abaixo)
```

**Módulos frontend sem index.ts** (devem ser importados por caminho completo):
`admin`, `auth`, `core`, `dashboard`, `fidelidade`, `inadimplencia`, `settings`

---

## Zustand Stores

Stores existentes (colocados em módulo/hooks):

- `modules/dentistas/hooks/useDentistasStore.ts`
- `modules/estoque/hooks/useEstoqueStore.ts` (~793 linhas — extrair ao adicionar)
- `modules/funcionarios/hooks/useFuncionariosStore.ts`
- `modules/pep/hooks/useOdontogramaStore.ts`
- `modules/procedimentos/hooks/useProcedimentosStore.ts`

---

## Arquivos Críticos — NÃO EDITAR

- `src/types/database.ts` — **~8929 linhas, autogenerated**. Regenerar via Prisma se necessário.

---

## Módulos Frontend (37)

`src/modules/`: admin, agenda, application, auth, bi, cobranca, contratos, core, crm, crypto, dashboard, dashboards, dentistas, domain, estoque, fidelidade, financeiro, funcionarios, ia-radiografia, inadimplencia, inventario, lgpd, marketing-auto, odontograma, orcamentos, pacientes, pdv, pep, portal-paciente, procedimentos, settings, split-pagamento, teleodonto, tiss, tratamentos, ui

---

## TS Errors Conhecidos (pré-existentes — não regredir)

- `crypto-pagamentos`: múltiplos módulos não encontrados (`@financeiro/components/*`, `./BitcoinInfoSection`) — alias `@financeiro` não mapeado no tsconfig
- `marketing-auto/IndicacoesTab.tsx`: variant string incompatível
- `dentistas/useDentistaForm.ts`: campo `horarioAtendimento` inexistente no tipo
- `usuarios/UserForm.tsx`: SubmitHandler generic mismatch
- `tour/`: `@ts-expect-error` inútil (deve ser removido, não é blocker)
- `financeiro/ConciliacaoBancaria.tsx`: `@ts-expect-error` inútil
- `auth/Auth.tsx`, `pacientes/PacientesListPage.tsx`: variant `"cta"` não existe no Button

---

## ESLint

- `eslint.config.js` na raiz desabilita muitas regras TS — verificar antes de assumir que regra está ativa
- `as any` e `@ts-ignore` existem amplamente — **não adicionar novos**
- Target: 0 errors, warnings tolerados (atualmente ~98)

---

## Correções de UI/CSS Aplicadas (2026-05-13)

### CSS Fantasmas → Criados em `src/index.css`

- `.glass-card` — 52 usos em 28 arquivos. Criado no `@layer components`.
- `.stat-card-premium` — 1 uso em `StatsCard.tsx`. Criado com gradiente de topo via `::before`.
- `.chart-card-premium` — 3 usos em `DashboardUnified.tsx`. Criado como card base com sombra.

### Sidebar Hardcoded → Refatorada para CSS vars

Arquivos modificados:

- `src/core/layout/Sidebar/SidebarMenuItem.tsx`
- `src/core/layout/Sidebar/SidebarHeader.tsx`
- `src/core/layout/Sidebar/SidebarFooter.tsx`

Substituições aplicadas:

- `emerald-500` → `hsl(var(--interactive))`
- `teal-400` → `hsl(var(--interactive-hover))`
- `emerald-50` → `hsl(var(--accent))`
- `emerald-700` → `hsl(var(--interactive))`
- `emerald-400` → `hsl(var(--interactive))`
- `emerald-600` → `hsl(var(--interactive))`

Todas as classes dark: redundantes foram removidas pois as CSS vars já respondem ao tema.

### Tokens v3 Integrados

- `tokensV3` exportado de `src/theme/tokens-v3.ts` agora disponível via `useTheme().tokensV3`.
- `src/contexts/ThemeContext.tsx` atualizado para incluir `tokensV3` no provider value.

### Arquivos Órfãos Removidos

- `src/components/ThemeProvider.tsx` — legado, não usado (App.tsx usa `contexts/ThemeContext.tsx`)
- `src/App.css` — legado Vite, nunca importado

### Conflito clinical.ts Documentado

- `src/themes/clinical.ts` recebeu WARNING no header: `applyClinicalTheme()` injeta vars inline que podem conflitar com `ThemeContext`.

## Build e Verificação

```bash
cd apps/web
pnpm dev          # Vite dev server (porta 3000)
pnpm build        # Build de produção
pnpm lint         # ESLint
npx tsc --noEmit  # Type-check (falhas pré-existentes acima são esperadas)
```
