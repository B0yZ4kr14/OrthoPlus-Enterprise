# OrthoPlus Enterprise — Master Audit Report (2026-05-29)

## Cycle 1 Complete — All Agents Executed

---

## 1. Brownfield Scan Results

### Critical
- dbRouter exposto em 6 modulos (configuracoes, crm, financeiro, inventario, pacientes, teleodonto)
- .env.vps.credentials existe no repo — requer auditoria
- Agent Service com CORS aberto (*) e sem autenticacao visivel

### High Impact
- 549 ocorrencias de prisma. diretamente nos modulos backend
- ~15 modulos backend anemicos (so api/, sem domain/ ou application/)
- God classes: FilesService.ts (684 linhas), pep/api/router.ts (652), tiss/controller.ts (530)

### Medium Impact
- ESLint excessivamente permissivo
- Zod v3 (backend) vs Zod v4 (frontend) mismatch
- 334 console.log no frontend, 90 no backend
- Threshold de cobertura 20%
- Alias @financeiro/* morto no tsconfig

---

## 2. Architecture Guard Results

### Critical (6)
- Controller direct Prisma access (10 modulos, ~119 calls)
- Memory Hub missing Zod validation
- Memory Hub drift endpoint bypasses Repository layer
- /rotate-key lacks admin auth
- /reindex missing clinicId
- PEP router has 30 direct Prisma calls

### High (6)
- 25 frontend components call apiClient directly
- any types in Memory Hub domain ports
- Missing audit logs in pacientes/financeiro/orcamentos
- Direct date-fns imports in 12 crypto files
- Direct localStorage access in 7 components

### Medium (4)
- Missing .specify/memory/tasks.md
- ContradictionDetector spec/task mismatch
- No OpenAPI schema
- No event schema registry

### Low (2)
- Misleading adminLimit rate limiter name
- Auth router lacks documented clinicGuard exception

**Constitution Compliance: ~78%**

---

## 3. Cleanup Results

### Fixed (Scout Rule)
- scripts/e2e-server.cjs — Prettier formatting
- tests/e2e/smoke.spec.ts — Prettier formatting

### Medium Issues (require design decisions)
- MD-001: Inconsistent browser console capture across E2E tests
- MD-002: Missing proxy timeout in e2e-server.cjs
- MD-003: Hardcoded backend URL in smoke.spec.ts
- MD-004: continue-on-error: true in E2E workflow

---

## 4. Verify Results

All 30 features verified — 100% task completion.

---

## 5. Security Review (from earlier cycle)

- SEC-001 MEDIUM: Hardcoded credentials in Playwright auth state
- SEC-002 LOW: Test secrets in plaintext CI YAML
- SEC-003 LOW: Debug JWT in git history
- SEC-004 LOW: Proxy error handler leaks backend messages

---

## Consolidated Action Plan

### P0 (Immediate)
1. Fix MemoryHubController Zod validation (V2)
2. Add admin auth to /rotate-key (V4)
3. Add clinicId validation to /reindex (V5)
4. Audit .env.vps.credentials for secrets
5. Sanitize e2e-server.cjs error responses (SEC-004)

### P1 (Short-term)
6. Add proxy timeout to e2e-server.cjs (MD-002)
7. Replace hardcoded credentials with runtime generation (SEC-001)
8. Extract Prisma direct access to repositories (549 occurrences)
9. Add missing audit logs in pacientes/financeiro/orcamentos
10. Migrate test secrets to GitHub Secrets (SEC-002)

### P2 (Medium-term)
11. Refactor god classes (FilesService, pep/router, tiss/controller)
12. Align Zod versions across stacks
13. Remove console.log statements (424 total)
14. Create OpenAPI schema
15. Create event schema registry
16. Increase test coverage threshold

### P3 (Architectural)
17. Enforce Clean Architecture boundaries
18. Standardize module structure (domain/application/infrastructure)
19. Implement proper Repository pattern consistently
20. Add API client abstraction layer in frontend

---

*Generated by Speckit Continuous Orchestration*
